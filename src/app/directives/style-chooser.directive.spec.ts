import { StyleChooserDirective } from './style-chooser.directive';

describe('StyleChooserDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleChooserDirective();
    expect(directive).toBeTruthy();
  });
});
